# Plan: Students Data Page with Pagination and Admin Delete

## Goal
Add a public students data page with 10 per page pagination, colorful cards, and admin delete functionality.

## Current State
- `/api/students` - GET/POST only, no DELETE
- `/students` - Public page with form only (no data display)
- `/admin/dashboard/students` - Shows students with 3 per page, NO delete

## Implementation Steps

### 1. Add DELETE endpoint to `/api/students/route.js`
- Add DELETE export function to delete a student by `_id`

### 2. Update Admin Students Page (`/admin/dashboard/students/page.jsx`)
- Change `itemsPerPage` from 3 to 10
- Add DELETE button to each student card
- Add delete handler that calls DELETE API
- Keep colorful styling (already has blue theme)

### 3. Create Public Students Data Page (`/students/data/page.jsx`)
- New page to display students with pagination (10 per page)
- Colorful cards (use gradient backgrounds or alternating colors)
- Pagination controls (Previous/Next buttons with page numbers)

## Clarifying Questions
1. **"image pagination"** - Do you mean:
   - Using icons/images for pagination buttons?
   - Or just standard numeric pagination?
   
2. **Color scheme** - Which colors do you prefer for the student cards?
   - Current: Blue/white theme
   - Options: Rainbow gradient, alternating colors, custom colors?

## Files to Modify/Create
- `src/app/api/students/route.js` - Add DELETE
- `src/app/admin/dashboard/students/page.jsx` - Update pagination & add delete
- `src/app/students/data/page.jsx` - Create new public page
